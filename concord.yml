flows:
  default:
    - task: "docker"
      name: "Run Shell Script in Docker"
      in:
        image: "library/alpine"
        cmd: |
          /bin/sh -c 'echo "Hello, ${name}"'

configuration:
  arguments:
    name: "world"

